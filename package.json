{
  "name": "xacti",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "yarn createHelpMessage && yarn ts-node-dev -r dotenv/config src/index.ts",
    "heroku-start": "rm tsconfig.json && npx tsc init && npx tsc --outDir ./dist && node dist/index.js",
    "start": "node dist/index.js",
    "build": "tsc",
    "createHelpMessage": "ts-node-dev scripts/createHelpMessage.ts"
  },
  "dependencies": {
    "discord.js": "^12.5.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.16",
    "cross-env": "^7.0.3",
    "dotenv": "^8.2.0",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.3"
  }
}