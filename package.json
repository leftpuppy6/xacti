{
  "name": "xacti",
  "version": "1.0.0",
  "main": "src/index.ts",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node-dev -r dotenv/config src/index.ts",
    "heroku-dev": "rm tsconfig.json && npx tsc init && npx tsc --outDir ./dist && node dist/index.js",
    "start": "node dist/index.js",
    "build": "tsc"
  },
  "dependencies": {
    "discord.js": "^12.5.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.16",
    "cross-env": "^7.0.3",
    "dotenv": "^8.2.0",
    "ts-node-dev": "^1.1.1",
    "typescript": "^4.1.3"
  }
}
